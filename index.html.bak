<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Magic Hands: Wonderland V11</title>
    
    <script src="https://cdn.jsdelivr.net/npm/@mediapipe/hands/hands.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/@mediapipe/camera_utils/camera_utils.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>

    <link rel="stylesheet" href="./style.css">
</head>
<body>
    <!-- 动态背景 -->
    <div id="bg-gradient"></div>

    <!-- 1. 首页：关卡选择 -->
    <div id="home-screen">
        <div class="logo-box">
            <h1>🌈 奇幻单词大冒险</h1>
            <p>Magic Wonderland</p>
        </div>
        
        <p class="hint-text">请选择一个关卡开始挑战：</p>
        
        <div class="level-grid">
            <button class="lvl-btn" onclick="window.selectLevel('animals')" style="--clr:#ff6b6b">
                🦁<br>动物森林
            </button>
            <button class="lvl-btn" onclick="window.selectLevel('food')" style="--clr:#feca57">
                🍔<br>美食天地
            </button>
            <button class="lvl-btn" onclick="window.selectLevel('vehicles')" style="--clr:#48dbfb">
                🚀<br>交通工具
            </button>
            <button class="lvl-btn" onclick="window.selectLevel('nature')" style="--clr:#1dd1a1">
                🌲<br>自然探索
            </button>
        </div>

        <div class="status-bar">
            <span id="cam-status">🎥 正在启动摄像头...</span>
        </div>
    </div>

    <!-- 2. 游戏 HUD -->
    <div id="game-hud">
        <div class="top-bar">
            <div class="score-pill">⭐ <span id="score">0</span></div>
            <button id="btn-back" onclick="window.location.reload()">🏠 返回</button>
        </div>
        
        <!-- 学习卡片弹窗 -->
        <div id="card-popup">
            <div class="card-body">
                <div id="c-icon">🍎</div>
                <h2 id="c-en">APPLE</h2>
                <h3 id="c-cn">苹果</h3>
                <div id="c-pron">/ˈæp.əl/</div>
                <div class="speaker-icon">🔊</div>
            </div>
        </div>
    </div>

    <!-- 3. 摄像头预览小窗 (方便调试) -->
    <video id="cam-feed" playsinline muted autoplay></video>
    
    <!-- 3D 场景 -->
    <canvas id="game-canvas"></canvas>

    <script type="module" src="./main.js"></script>
</body>
</html>